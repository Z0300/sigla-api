spring:
  config:
    import: "optional:file:.env[.properties]"
  datasource:
    url: jdbc:mysql://localhost:3306/springcore_db?createDatabaseIfNotExist=true
    username: root
    password: Chj7o1mXGCn0bbPsQcDu
  jpa:
    show-sql: true
  client:
    baseUrl: ${CLIENT_BASE_URL}